class Ball {
  constructor(x, y, width, height, angle) {
    var options = { 
      density: 1, 
      frictionAir: 0.005
    };
    
    //create properties & add to the world
   
  }

  display() {
    var angle = this.body.angle;
    //display in ellipse shape
    
  }
}
